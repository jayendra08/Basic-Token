import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['svc-da5997ff-morphvm-4et2drbc.http.cloud.morph.so']
};

export default nextConfig;
